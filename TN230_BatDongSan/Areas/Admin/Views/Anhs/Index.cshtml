@model IEnumerable<DbContextModel.Framework.Anh>

@{
    ViewBag.Title = "Index";
}

<p>
    @Html.ActionLink("Thêm ảnh mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaAnh)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DuongDan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DuongDan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DuongDan)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaAnh }) |
            @Html.ActionLink("Xem chi tiết", "Details", new { id=item.MaAnh }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaAnh })
        </td>
    </tr>
}

</table>
